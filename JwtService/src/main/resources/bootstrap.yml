spring:
  application:
    name: JwtService
  cloud:
    config:
      profile: native
      uri: http://localhost:9010/
  rabbitmq:
    host: localhost
    port: 5672
  zipkin:
    base-url: http://localhost:9030