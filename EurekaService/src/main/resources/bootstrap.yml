spring:
  application:
    name: EurekaService
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:9000/
  rabbitmq:
    host: localhost
    port: 5672
  zipkin:
    base-url: http://localhost:9100