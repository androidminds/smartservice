spring:
  application:
    name: EurekaService
  cloud:
    config:
      profile: native
      uri: http://localhost:9000/
  rabbitmq:
    host: localhost
    port: 5672
  zipkin:
    base-url: http://localhost:9700