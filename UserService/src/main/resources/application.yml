logging:
  level:
    com.androidminds.springstudy: DEBUG
    org.springframework.web: DEBUG
    org.hibernate: DEBUG
  file:

server:
  port: 9200

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/

userservice:
  max-page-count: 50
